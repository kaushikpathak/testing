'use strict'

const IS_REQUIRED_MESSAGE = 'is required.';

const Constants = {
    ERROR_MSG: {
        // getRequiredMsg: (key) => `${key} ${IS_REQUIRED_MESSAGE}`,
        INVALID_URL: 'Invalid url is passed!'
    },
    PROJECT_END_POINT: 'projects',
    CONTACT_END_POINT: 'contacts',
    COMPANY_END_POINT: 'companies',
    MESSAGE_END_POINT: 'messages'
};

module.exports = Constants;
